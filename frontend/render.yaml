services:
  - type: static
    name: wish-market-place-front
    env: static
    buildCommand: npm run build
    staticPublishPath: frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html